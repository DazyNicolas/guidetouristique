{% extends 'base.html.twig' %}

{% block title publication.titre %}

{% block body %}

<article>
        <h1>{{publication.titre}}</h1>
        <p>{{publication.description}}</p>
       
</article>
<a href="{{path('app_publication_edit', {'id': publication.id})}}">Moddifier la publication</a>
<a href="#" onClick="event.preventDefault(); confirm('Vous voulez vraiment supprimer cette publication') && document.querySelector('#js-publication-delete-form').submit()">Supprimer</a>
<a href="{{path('app_publications')}}">Rétour vers tous les publications</a>


 <form id="js-publication-delete-form" action = "{{path('app_publication_delete', {'id': publication.id})}}" method="post" style="display:none;">
        <input type="hidden" name="csrf_token" value="{{csrf_token('publication_deletion_' ~ publication.id)}}">
        <input type="submit" value = "Supprimer">
</form>
{% endblock %}
